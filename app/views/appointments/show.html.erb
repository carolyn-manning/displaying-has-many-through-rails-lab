<h2> <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> </h2>

<p>
    <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %>  <br>
    <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %> 
<p>

